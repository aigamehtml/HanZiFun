{
  "name": "hanzifun",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Offline-first Chinese handwriting worksheet generator",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "assets": "node scripts/build-icons.mjs",
    "data": "node scripts/build-stroke-data.mjs",
    "data:core": "node scripts/build-stroke-data.mjs --core-only",
    "build": "npm run assets && npm run data && node scripts/build.mjs",
    "build:quick": "npm run assets && npm run data:core && node scripts/build.mjs",
    "check": "node --check app.js && node --check service-worker.js && node --check scripts/build-stroke-data.mjs && node --check scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "hanzi-writer-data": "2.0.1",
    "html-minifier-terser": "^7.2.0",
    "pinyin-pro": "^3.27.0"
  }
}
